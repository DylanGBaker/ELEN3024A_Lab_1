function x = testing_stuff()
fs = 100;
t = 0:1/fs:2;
m =  4*cos(100*pi*t) + 3*sin(200*pi*t) + 3*sin(300*pi*t);\
subplot(3,1,1);
plot(t,m);
u = cos(1000*pi*t);
subplot(3,1,2);
plot(t,u);

M = fftshift(fft(m)); 
n = length(m) - 1;
df = fs/n;
f = -fs/2:df:fs/2;
subplot(3,1,2);
plot(f,abs(M)/n);
end